apiVersion: v1

kind: Pod
metadata:
  name: testpod
  label:
    tier: frontend
spec:
  containers:
  - name: hello
    image: busybox
    command: ['sh', '-c', 'echo "Hello, Kubernetes!" && sleep 3600']
