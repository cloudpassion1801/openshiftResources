apiVersion: v1
kind: ReplicationController
metadata:
  name: my-rc
spec:
  replicas: 3
  selector:
    tier: frontend
  template:
    metadata:
      name: somename
      labels:
        app: myapp
        tier: frontend
    spec:
      containers:
       - name: hello
         image: busybox
         command: ['sh', '-c', 'echo "Hello, Kubernetes!" && sleep 3600']
